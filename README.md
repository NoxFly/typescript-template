# Typescript Template

## Dependencies installation & run
```sh
npm i
# default run
npm run dev
# live reload with nodemon
npm run live
```

## Paths

* `~/` : `src/`
* `~~/` : `/`
* `~/shared/` : `src/shared/`